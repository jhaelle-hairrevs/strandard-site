# Strandard Production Final (Static)

## What's inside
- index.html
- privacy-policy.html (served at /privacy-policy via _redirects)
- delete-account.html (served at /delete-account via _redirects)
- /css/styles.css
- /js/app.js
- /assets/logo.svg (replace with your real logo if desired)

## Important
All asset links use ABSOLUTE paths (start with /) to prevent broken CSS on subpages.

## Deploy
Upload the contents of this folder to Cloudflare Pages (or any static host).
